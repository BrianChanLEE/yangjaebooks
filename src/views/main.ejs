<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>YangJae</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #000428, #004e92);
            perspective: 1500px;
            overflow-x: hidden;
            overflow-y: auto;
            position: relative;
        }
        .stars {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: transparent url('https://cms-imgp.jw-cdn.org/img/p/500100114/KO/art/500100114_KO_lsr_lg.jpg') center center/cover no-repeat;
            filter: blur(2px) brightness(0.6);
            animation: twinkle 4s ease-in-out infinite;
            z-index: 0;
        }
        .container {
            text-align: center;
            padding: 40px;
            background: rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.6);
            transform: rotateY(20deg) rotateX(10deg);
            animation: float 8s ease-in-out infinite;
            z-index: 1;
            backdrop-filter: blur(10px);
            cursor: pointer;
            max-width: 90%;
            width: 400px;
            height: auto;
        }
        h1 {
            color: #f8f8f8;
            font-size: 2.2em;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
            margin: 0;
        }
        p {
            color: #eeeeee;
            font-size: 1.2em;
            font-style: italic;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.6);
            margin-top: 10px;
        }
        @keyframes float {
            0% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
            50% { transform: rotateY(20deg) rotateX(10deg) translateY(-10px); }
            100% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
        }
        @keyframes glow {
            0% { text-shadow: 0 0 20px rgba(255, 255, 255, 0.8); }
            100% { text-shadow: 0 0 30px rgba(255, 255, 255, 1); }
        }
        @keyframes twinkle {
            0%, 100% { opacity: 0.8; }
            50% { opacity: 1; }
        }

        /* 반응형 처리를 위한 미디어 쿼리 */
        @media (max-width: 768px) {
            body {
                padding: 20px;
                height: auto;
            }
            .container {
                width: 100%;
                max-width: 90%;
                transform: none;
            }
            h1 {
                font-size: 1.8em;
            }
            p {
                font-size: 1em;
            }
        }
    </style>
</head>
<body>
    <div class="stars"></div>
    <div class="container" id="clickBox">
        <h1>Books Inventory App</h1>
        <!-- <p>Ac 5:19~21,42</p> -->
    </div>

    <script>
        const clickBox = document.getElementById('clickBox');

        clickBox.addEventListener('click', () => {
            window.location.href = '/three'; // 페이지 이동
        });
    </script>
</body>
</html>
